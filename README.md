# crm-django

1. git clone https://github.com/joseFelipe/crm-django.git

2. python -m venv venv (Create virtual env)

3. .\venv\Scripts\Activate.ps1 (Enter the virtual env)

4. pip install django

5. pip install django-filter

6. pip install pillow